# 构建产物与依赖
node_modules/
dist/
coverage/

# 发布工作流在打包机上把 dist/main.js 复制到根目录再挂到 Release
# （目录按文件名下载资产，必须叫 main.js）。本地跑同样步骤时别把它提交进来 ——
# 本仓库的源码在 src/，根目录的 main.js 永远是产物、不是源文件。
main.js

# 运行时状态：记录 vault 内的笔记路径与使用痕迹，属用户数据而非插件代码。
# 它由 Obsidian 写在 <vault>/.obsidian/plugins/<id>/data.json，正常不会进仓库，
# 但手工拷贝插件目录时很容易带进来 —— 显式排除掉。
data.json

# 日志与编辑器临时文件
*.log
.DS_Store
Thumbs.db
